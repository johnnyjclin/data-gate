在本機啟動 MkDocs 預覽伺服器，瀏覽知識庫網站。

## 使用方式

```bash
mkdocs serve
```

啟動後在瀏覽器開啟 http://127.0.0.1:8000

## 說明

- 伺服器會在背景持續執行，檔案變更會自動重新整理
- 按 Ctrl+C 停止伺服器
- 如果 8000 port 被占用，使用：
  ```bash
  mkdocs serve --dev-addr 127.0.0.1:8001
  ```
